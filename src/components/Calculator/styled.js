import styled from "styled-components"

export const Retornar = styled.div`
    display: flex;
    align-items: start;
    button {
            color: white;
            border: 0;
            border-radius: 0.25rem;
            background-color: #164194;
            padding: 0.5rem;
        }
`